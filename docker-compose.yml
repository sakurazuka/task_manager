version: '2'
services:
  task_manager_db:
    image: postgres
  task_manager_back:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/task_manager
    ports:
      - "3000:3000"
    depends_on:
      - task_manager_db
